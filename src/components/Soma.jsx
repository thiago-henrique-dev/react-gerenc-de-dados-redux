import React from 'react'
import Card from './Card'

const Soma = props => {

    return (
        <Card title="Soma dos números" blue>
            <div>
                <span>
                    <span>Resultado: </span>
                  <strong>{5}</strong>

                 </span>
       
            </div>

            
        </Card>
    )
}

export default Soma